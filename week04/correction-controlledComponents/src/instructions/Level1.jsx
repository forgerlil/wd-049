const Level1 = () => {
  return (
    <>
      <h4>Controlling the inputs</h4>
      <ul>
        <li>
          You have a few components ready for you to use. Go to <b>Form.js</b>{' '}
          and analyse the component. Create states to track the inputs in the
          form.
        </li>
        <li>
          Listen to changes on each of the inputs and store the values on the
          states you have created.
        </li>
      </ul>
    </>
  );
};

export default Level1;
