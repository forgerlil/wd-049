const Bonus = () => {
  return (
    <>
      <h3>BONUS SECTION</h3>
      <h4>Multiple inputs, one state</h4>
      <ul>
        <li>
          Refactor the states that control the inputs to a single state that
          controls every input in the form.
        </li>
      </ul>
    </>
  );
};

export default Bonus;
